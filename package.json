{
  "name": "coa-websocket",
  "version": "0.0.0",
  "description": "一个超轻量的WebSocket服务框架，COA核心组成之一",
  "keywords": [
    "coajs",
    "coa",
    "coa-websocket",
    "websocket",
    "ws"
  ],
  "license": "MIT",
  "author": "Aex",
  "homepage": "https://github.com/coajs",
  "repository": {
    "type": "git",
    "url": "https://github.com/coajs/coa-websocket.git"
  },
  "scripts": {
    "dev": "tsc -w",
    "build": "rm -rf dist && tsc && cp package.json *.md dist",
    "publish-prerelease": "yarn build && yarn version --prerelease && cp package.json *.md dist && cd dist && npm publish"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/node": "^14.14.37",
    "typescript": "^4.2.3"
  }
}